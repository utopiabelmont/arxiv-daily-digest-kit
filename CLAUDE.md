# For Claude Code

If the user asked you to **install / set up this automation**, open `INSTALL.md`
and follow it as the installer playbook. Claude users may choose Backend A
(Claude cloud Routine) or Backend B (GitHub Actions cron); see INSTALL.md Step 0.
