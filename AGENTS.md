# For AI agents (Codex, etc.)

If the user asked you to **install / set up this automation**, open `INSTALL.md`
and follow it as the installer playbook. You are running as the installer agent.
Default execution backend for Codex users: **Backend B (GitHub Actions cron)** —
see INSTALL.md Step 2B. Do not attempt to create Claude cloud routines.
